{% extends 'base.html.twig' %}

{% block title %}Admin - Nouvel Utilisateur{% endblock %}

{% block body %}
    <main class="flex">
        {%  include 'layout/sidebar.html.twig' %}
        <div class="flex flex-col w-full m-6">
            <h1 class="mt-3 mb-6 text-center text-xl font-bold">Créer un Utilisateur</h1>

            <div class="flex flex-col justify-center">
                {{ include('admin/user/user_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                <div class="flex justify-between">
                    <a class="" href="{{ path('admin_user_list') }}">< Retour à la liste</a>
                    <a class="pl-4" href="{{ path('app_admin_dashboard') }}">< Retour au tableau de bord</a>
                </div>
            </div>
        </div>
    </main>

{% endblock %}