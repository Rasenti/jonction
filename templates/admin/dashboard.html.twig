{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

  <main class="flex">
        {% include 'layout/sidebar.html.twig' %}

        <div class="flex flex-col w-full m-6">
            <div class="flex pb-6 pl-1">
                <a href="{{ path('admin_user_list') }}">
                    <button class="theme-button">Utilisateurs</button>
                </a>
            </div>
            <div class="flex pb-6 pl-1">
                <a href="{{ path('admin_page_list') }}">
                    <button class="theme-button">Pages</button>
                </a>
            </div>
            <div class="flex pb-6 pl-1">
                <a href="{{ path('admin_category_list') }}">
                    <button class="theme-button">Catégories</button>
                </a>
            </div>
        </div>
    </main>

{% endblock %}